<en-US>
• SFTP: a built-in editor for the files on your server — text files are edited in place and keep their encoding, line endings and permissions, configuration files are syntax coloured, and anything that is not text opens in a hex editor
• SSH keys: a key with a passphrase now works — the passphrase is asked once when you import the key and is not stored anywhere. Keys imported earlier must be imported again; the list marks them.
</en-US>

<it-IT>
• SFTP: editor integrato per i file sul server — i file di testo si modificano sul posto e mantengono codifica, fine riga e permessi, i file di configurazione vengono colorati, e ciò che non è testo si apre in un editor esadecimale
• Chiavi SSH: una chiave con passphrase ora funziona — la passphrase viene chiesta una volta sola all'importazione e non viene salvata da nessuna parte. Le chiavi importate prima vanno reimportate; la lista le segnala.
</it-IT>

<de-DE>
• SFTP: eingebauter Editor für die Dateien auf dem Server — Textdateien werden direkt dort bearbeitet und behalten Kodierung, Zeilenenden und Rechte, Konfigurationsdateien werden farbig hervorgehoben, und alles, was kein Text ist, öffnet im Hex-Editor
• SSH-Schlüssel: ein Schlüssel mit Passphrase funktioniert jetzt — die Passphrase wird beim Import einmal verlangt und nirgends gespeichert. Früher importierte Schlüssel neu importieren; die Liste zeigt sie.
</de-DE>

<es-ES>
• SFTP: editor integrado para los archivos del servidor — los de texto se editan allí mismo y conservan codificación, fin de línea y permisos, los de configuración se colorean, y lo que no es texto se abre en un editor hexadecimal
• Claves SSH: una clave con contraseña ya funciona — la contraseña se pide una sola vez al importarla y no se guarda en ningún sitio. Las claves importadas antes hay que importarlas de nuevo; la lista las marca.
</es-ES>

<fr-FR>
• SFTP : éditeur intégré pour les fichiers du serveur — les fichiers texte se modifient sur place en conservant encodage, fins de ligne et droits, les fichiers de configuration sont colorés, et ce qui n'est pas du texte s'ouvre dans un éditeur hexadécimal
• Clés SSH : une clé avec phrase secrète fonctionne enfin — la phrase est demandée une fois à l'import et n'est enregistrée nulle part. Les clés importées avant doivent être réimportées ; la liste les signale.
</fr-FR>

<pt-PT>
• SFTP: editor integrado para os ficheiros no servidor — os de texto são editados no próprio servidor e mantêm codificação, fins de linha e permissões, os de configuração são coloridos, e o que não é texto abre num editor hexadecimal
• Chaves SSH: uma chave com frase-passe já funciona — a frase-passe é pedida uma vez ao importar e não é guardada em lado nenhum. As chaves importadas antes têm de ser importadas de novo; a lista assinala-as.
</pt-PT>

<uk>
• SFTP: вбудований редактор для файлів на сервері — текстові файли редагуються на місці й зберігають кодування, кінці рядків і права, файли налаштувань підсвічуються, а все, що не є текстом, відкривається в шістнадцятковому редакторі
• Ключі SSH: ключ із парольною фразою тепер працює — фразу запитують один раз під час імпорту й ніде не зберігають. Раніше імпортовані ключі потрібно імпортувати знову; список їх позначає.
</uk>

<ru-RU>
• SFTP: встроенный редактор для файлов на сервере — текстовые файлы правятся прямо там и сохраняют кодировку, переводы строк и права, файлы настроек подсвечиваются, а всё нетекстовое открывается в шестнадцатеричном редакторе
• Ключи SSH: ключ с паролем теперь работает — пароль спрашивают один раз при импорте и нигде не сохраняют. Ранее импортированные ключи нужно импортировать заново; список их отмечает.
</ru-RU>

<zh-CN>
• SFTP：内置编辑器，可直接编辑服务器上的文件——文本文件就地修改，编码、换行符和权限保持不变；配置文件带语法着色；非文本文件在十六进制编辑器中打开
• SSH 密钥：带密码短语的密钥现在可以使用——导入时只询问一次密码短语，且不会保存在任何地方。此前导入的密钥需要重新导入，密钥列表会标出。
</zh-CN>

<ja-JP>
• SFTP: サーバー上のファイルを直接編集できるエディタを内蔵しました。テキストファイルは文字コード・改行コード・権限をそのままに編集でき、設定ファイルは色分け表示され、テキストでないファイルは16進エディタで開きます
• SSH鍵: パスフレーズ付きの鍵が使えるようになりました。パスフレーズはインポート時に一度だけ尋ね、保存しません。以前にインポートした鍵は再インポートが必要で、一覧に印が付きます。
</ja-JP>
